apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: martin-nginx-demo
  labels:
    app: martin-nginx-demo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: martin-nginx-demo
