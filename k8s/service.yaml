apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: Martin-Argo1
  labels:
    app: Martin-Argo1
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: Martin-Argo1
