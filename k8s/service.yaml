apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: martin-argo
  labels:
    app: martin-argo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: martin-argo
